DescriptorManager
	DescriptorLayout
	DescriptorPool
	DescriptorSet

Descriptor
	count
	stageFlag
	pImmutableSamplers

UBO(0)
IMG(1)

Vert
binding=0
binding=1
binding=4
Smapler binding= 3

Frag
binding=2

descriptorManager.addLayout(UBO, UBO, IMG, UBO_DYN, UBO_ARR)
descriptorManager.init() // Create pool from layouts

descriptorManager.updateBufferDesc(0, 0, buffer, offset, range)
descriptorManager.updateBufferDesc(0, 1, buffer, offset, range)

descriptorManager.updateBufferDesc(setIndex, binding, buffer, offset, range)
descriptorManager.updateImageDesc(setIndex, binding, imgLayout, imgView, sampler)

descriptorManager.updateSets()

descriptorManager.cleanup()